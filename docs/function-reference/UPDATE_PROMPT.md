# Update Prompt for LUMI Function Reference

Use this prompt to update the function reference files under `docs/function-reference/`.

---

Update the LUMI function reference in `docs/function-reference/` for the files or functions listed below.

- Use the same CHANGELOG-style format: full code snippet, file path, line numbers, purpose, and explanation.
- Categorize each entry under `Backend`, `Frontend`, `Misc`, or `Geolocation`.
- Use direct, positive language. Avoid litotes.
- If a new category is added, update `docs/function-reference/README.md` and regenerate `DOCS_INDEX.md` so the docs stay sorted.

Files/functions to update:
<!-- list them here -->
