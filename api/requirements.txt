# Vercel Python function dependencies (light subset of fastapi-backend/requirements.txt).
# Excludes the heaviest ML/RAG packages that push the deployment bundle over Vercel's limits:
#   - sentence-transformers (and its torch/transformers dependency)
#   - faiss-cpu
#   - statsmodels (and its scipy dependency)
#
# On-demand ARIMA retraining is disabled on Vercel Free; pre-computed forecasts
# come from the doe_datasets table.

fastapi==0.115.0
pydantic-settings==2.4.0
python-dotenv==1.0.1
supabase==2.10.0
python-jose[cryptography]==3.3.0
httpx==0.27.2
redis==5.0.8
google-genai==0.6.0
groq==0.18.0

# Data processing
pandas>=2.0.0
numpy>=1.24.0
