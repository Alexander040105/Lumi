# Vercel Python function dependencies (light subset of fastapi-backend/requirements.txt).
# Excludes the heaviest ML/RAG packages that push the deployment bundle over Vercel's limits:
#   - sentence-transformers (and its torch/transformers dependency)
#   - faiss-cpu
#
# Keep statsmodels/scipy for the forecast endpoints; the combined installed size is ~350 MB.

fastapi==0.141.1
pydantic-settings==2.4.0
python-dotenv==1.2.2
supabase==2.10.0
PyJWT[crypto]==2.13.0
httpx>=0.26,<0.28
redis==5.0.8
cryptography==50.0.0
groq==0.18.0

# Time-series forecasting
statsmodels>=0.14.0

# Data processing
tabulate>=0.9.0
pandas>=2.0.0
numpy>=1.24.0
