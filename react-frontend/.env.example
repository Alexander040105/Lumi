VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# On Vercel this should be /api/v1 when the backend is in the same project,
# or the full production API URL (e.g. https://lumi-api.vercel.app/api/v1).
VITE_API_BASE_URL=/api/v1
