[pytest]
markers =
    mock: Tests that use mocked dependencies (no live backend required)
    live: Tests that require a running FastAPI backend server
    local: Tests that can run offline without external services
    benchmark: Performance benchmark tests
